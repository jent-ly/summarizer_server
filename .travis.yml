dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"

install:
  - make install
  - python3.7 -c "import nltk; nltk.download('punkt')"

script:
  - black --check .
  - make test
