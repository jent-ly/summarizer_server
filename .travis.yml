language: python

services:
  - docker

before_install:
  - docker-compose up -d --build api
  - docker ps -a
  - docker exec summarizer-server sh -c "cd /server && black --check ."

script:
  - black --check .
